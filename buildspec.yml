version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
      - ls -R
artifacts:
  name: artifacts-test-build#$CODEBUILD_BUILD_NUMBER
  files:
    - '*.*'
  base-directory: artifacts/my-build-2
